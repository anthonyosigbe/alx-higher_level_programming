#!/usr/bin/node
/* Specifies a class named Rectangle with attributes for width and height. */

module.exports = class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
};
